# LESSONS.md — AI 灵感簿项目复盘

> v1.0 上线日期：2026-06-23  
> 复盘作者：Essie Zhang（产品经理）

---

## 一、项目概述

AI 灵感簿是一个「隔天从 GitHub、YouTube、X 自动采集 AI 小项目，AI 筛选+分析，生成静态网页」的个人产品灵感工具。项目经历了从 0 到 1 的完整搭建：数据管线 → 分析框架 → 网页交互 → 多 Agent 协作 → GitHub Pages 部署。

---

## 二、关键挑战与解决方案

### 2.1 中文乱码问题（反复出现）

**现象**：卡片内容出现大量问号乱码，研发每次修完过几天又复发。

**根因**：Python 和 Node.js 对 UTF-8 编码的处理方式不同。Python 脚本写文件时默认用系统编码（Windows 下可能不是 UTF-8），而 Node.js 的 `fs.writeFileSync` 需要显式声明 UTF-8。

**最终方案**：
- `projects.json` 的写入统一用 Node.js + `Buffer.from(content, 'utf-8')`
- 严禁用 Python 脚本碰 `projects.json` 和 `index.html`
- 页面生成器固定用 `node scripts/generate_page.js`，放弃 Python 版本

**教训**：从项目一开始就统一编码工具链。多语言混用时，明确一个「正源」——谁能写核心数据文件，谁只能读。

---

### 2.2 研发"回报完成"但实际未生效

**现象**：研发回报「已重新生成 index.html」，但 PM 打开本地页面发现内容没变过。

**根因**：研发在对话里写了代码修改、跑了语法检查、commit 了，但没有实际执行 `node scripts/generate_page.js`。「改代码」≠「执行代码」。

**最终方案**：验收标准明确为「PM 在浏览器里看到变化」，而非「代码已修改/已 commit」。PM 侧强制硬刷新（Ctrl+Shift+R）排除浏览器缓存干扰。

**教训**：验收只看结果，不看过程。代码改了、commit 了、语法检查通过了都不等于「完成了」。必须 PM 亲眼看到页面变化才算数。

---

### 2.3 Agent 角色职责边界不清晰

**现象**：产品经理 Agent 在验收时发现 bug，自己上手修了代码，导致研发那边产生冲突。反过来研发 Agent 也有时越界做了内容编辑的事。

**根因**：没有严格执行「数据所有权」——谁写 `projects.json`，谁写 `index.html`，谁只读不写。

**最终方案**：
- 写入 `projects.json` 和 `index.html`：只允许产品研发 Agent
- 写入 `data/opportunities/*.md`：产品经理 Agent（蒸馏工作）
- 采集 Agent：只写 `data/raw/` 和候选清单
- PRD 明确规定了哪些文件属于哪个角色

**教训**：多 Agent 协作的核心不是「大家都能干活」，而是「谁的东西谁碰，别的人只看不写」。

---

### 2.4 Web 字体显示不一致

**现象**：全站各处字号大小不统一——卡片标签 8px，正文 15px，Key Insight 标签 8px，分析正文 14px→13px，导航 13px，各有各的设定。

**根因**：没有字体规范系统，每个组件各自定义字号。

**最终方案**：建立六级字体阶梯（12/13/14/15/18/22px），所有组件统一从阶梯中取值。字族也明确：Serif 用于标题和正文，Sans 用于 UI 标签按钮导航。

**教训**：视觉规范要在项目初期就定，不要等到组件多了再回头改。改一处的代价是逐行排查所有 CSS。

---

### 2.5 BuilderPulse 内容与目标用户不匹配

**现象**：Opportunities 频道直接渲染 BuilderPulse 原文，内容全是技术黑话——npm install、Iroh endpoints、GPU kernels、MCP 协议。普通用户看了跟没看一样。

**根因**：BuilderPulse 的目标读者是独立开发者/技术构建者，不是 AI 产品新手。但我们直接把它搬过去了。

**最终方案**：新增蒸馏步骤——从 BuilderPulse 原文中只提取「模型进展 / 产品发布 / 对日常有什么帮助」相关的内容，用平实中文重写，每条控制在 5-6 句。删掉纯技术工程板块（今日2小时构建、技术选型等）。

**教训**：引用别人的内容时，第一件事不是「怎么渲染」，而是「这个东西是写给谁的」。原内容的目标读者不等于我们的目标读者。

---

### 2.6 网页拖拽问题（浏览器误识别）

**现象**：鼠标在页面上移动时偶尔会触发选中文档拖拽的效果。

**根因**：CSS 的 `user-select: none` 未覆盖到 HTML 元素本身。部分浏览器在未明确禁止拖拽时会把鼠标移动识别为拖拽操作。

**最终方案**：在 CSS 根元素添加 `html { user-select: none; -webkit-user-select: none; }`，同时在可输入区域（如搜索框）单独恢复 `user-select: text`。

**教训**：前端交互的小细节越早发现越容易修。不要让这类体验问题积压到后期。

---

## 三、做得好的地方

1. **PRD 驱动开发**：所有需求先写进 PRD，研发按 PRD 实现。即使 PRD 被改了很多版，始终是唯一的源头。
2. **SOP 消息格式**：`📋 派发 / ✅ 完成 / ⚠️ 退回 / 🎉 验收` 四态协议让跨 Agent 沟通清晰高效。
3. **备份回滚机制**：`projects_backup.json` + 自动备份 `index.html`，每次改动前都有回退路径。
4. **质量过滤**：YouTube MIN_VIEWS=500 + 优质频道白名单 + 零信号不入库，这套规则避免了低质内容进入最终页面。
5. **蒸馏思维**：不是「搬运」别人的内容，而是「转译」成自己用户能看懂的语言——这个思路贯穿了整个项目。

---

## 四、如果再做一遍会怎么做

1. **先建字体规范，再写组件**。避免后期逐行改 CSS 的重复劳动。
2. **Agent 创建时就定好文件权限**。谁写什么文件，写在 AGENTS.md 里，不要等出问题了再补。
3. **验收标准直接从 PRD 里写清楚**。「代码改了」不算完成，「PM 在浏览器里看到效果」才算。
4. **源内容先评估目标读者匹配度**。引用 BuilderPulse 之前，先问「这个东西是写给谁的」。
5. **编码问题第一天解决**。Python vs Node.js 的 UTF-8 差异是已知坑，不用等到出现乱码再处理。

---

## 五、可复用资产

- **PRD 模板**：`_system/planning/PRD.md` — 多平台数据管线 + 分析框架 + 视觉系统 + 部署方案的完整 PRD
- **SOP 协议**：`AGENTS.md` — 多 Agent 协作的消息格式、触发规则、文件所有权协议
- **通信录**：`_system/通信录.md` — Agent 线程 ID 注册表 + 协作链路图
- **分析框架**：PRD §3 — Pain Point / Why AI / Product Insight / Transfer 四块分析法 + 语言风格规则
- **字体规范**：PRD §6.6 — 六级字号阶梯 + 字族分配

---

*Designed by Essie Zhang*

---

## 六、Token 消耗优化复盘

以下对照常见 Token 浪费模式，逐一复盘本项目的实际情况：

### 6.1 已犯的错误

**对话不 /clear（🔴 严重）**
- 本项目对话从第一天到上线未清理，上下文持续膨胀。每次需要查 PRD、CSS、工作日志时都要重新扫。
- **优化方向**：项目完成一个阶段（如一个频道上线）后清理上下文。后续对话只需读 docs 索引，不重扫全部文件。

**改错了不回退（🟡 偶发）**
- 研发多次出现：修一个 bug 把之前修好的另一个 bug 带回来（最典型的是中文乱码反复出现）。
- 我们已加了 `projects_backup.json` 和自动备份机制，但研发有时不先回退，而是在错误版本上累加修复，导致思路越来越绕。
- **优化方向**：验收发现问题时，先 `git checkout` 回干净版本，再重新修。不在错的基础上叠修正。

**AGENTS.md / PRD 写太长（🟡 轻度）**
- AGENTS.md 和 PRD 都较详细，PRD 目前 19000+ 字。每次新对话都要读。
- **优化方向**：AGENTS.md 压缩为短规则清单（一行一条），详细内容放 `_system/docs/` 下按需读取。PRD 拆为核心规约 + 实现细节两个文件。

### 6.2 做得好的地方

- **没有装多余的 MCP / skill** — 只用 follow-builders 和 content-extractor，按需安装
- **没有用 cat 读大文件** — 始终用 Select-String 或 Select-Object -First 限制输出
- **没有重复贴代码报错** — 回报用 ID 追踪，不贴大段代码
- **大任务用 subagent** — 三个 Agent 分工明确（采集/产品/研发），各司其职
- **使用 grep 搜索定位** — 始终先搜索定位再读取具体行

### 6.3 后续可执行的优化项

| 优先级 | 优化项 | 操作 |
|--------|--------|------|
| 🔴 | 精简 AGENTS.md | 压缩为 20 行以内规则清单 |
| 🔴 | PRD 分层 | 核心规约（<5000字）+ 实现细节分开 |
| 🟡 | 定期 /clear | 每完成一个频道/大功能后清理 |
| 🟡 | 回退优先 | 验收遇 bug 先 git checkout，不累积修复 |
| 🟢 | docs 索引 | 后续项目一启动就建 docs，不每次扫全项目 |

---

*Designed by Essie Zhang*
